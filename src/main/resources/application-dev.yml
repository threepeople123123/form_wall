spring:
  ai:
    dashscope:
      # 需要替换为你自己的 key
      api-key: sk-0363d3e4787e4ab19253e56309e0ff95
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      chat:
        options:
          model: deepseek-v4-flash
    ollama:
      base-url: http://localhost:11434
      chat:
        model: gemma3:1b
    chat:
      client:
        enabled: false
  data:
    # redis配置
    redis:
      # Redis数据库索引（默认为0）
      database: 1
      # Redis服务器地址
      host: 8.140.219.233
      # Redis服务器连接端口
      port: 6379
      # Redis服务器连接密码（默认为空）
      password: wangjian020426
      # 连接超时时间
      timeout: 10s
      lettuce:
        pool:
          # 连接池最大连接数
          max-active: 100
          # 连接池最大阻塞等待时间
          max-wait: 2000ms
          # 连接池中的最大空闲连接
          max-idle: 30
          # 连接池中的最小空闲连接
          min-idle: 10
      connect-timeout: 10s
  datasource:
    url: jdbc:postgresql://8.140.219.233:5432/campus-future
    username: postgres
    password: wangjian020426
    driver-class-name: org.postgresql.Driver
    # 可选：连接池配置 (推荐使用 HikariCP，Spring Boot 默认)
    hikari:
      # 最小空闲连接数
      minimum-idle: 5
      # 最大连接数
      maximum-pool-size: 20
      # 连接超时时间（毫秒）
      connection-timeout: 30000
      # 连接最大存活时间（毫秒）
      max-lifetime: 1800000
  mail:
    host: smtp.qq.com
    port: 465 # 或者 587
    username: 1994814695@qq.com
    password: lpclwlacqopldgca
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          # 如果使用 465 端口，通常需要启用 SSL
          ssl:
            enable: true
  jackson:
    serialization:
      # 建议禁用这个，否则日期会变成一串数字数组 [2023,10,27...]
      write-dates-as-timestamps: false
  # RabbitMQ 配置
  rabbitmq:
    host: 8.140.219.233  # RabbitMQ 服务器地址
    port: 5672       # RabbitMQ 端口
    username: admin  # 用户名
    password: password123  # 密码
    virtual-host: /  # 虚拟主机
    listener:
      simple:
        # 消费者最小线程数
        concurrency: 5
        # 消费者最大线程数
        max-concurrency: 10
        # 手动确认消息
        acknowledge-mode: manual
        # 每次从队列中取出的消息数
        prefetch: 1
############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token 名称（同时也是 cookie 名称）
  token-name: token
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: false
  # token 风格（默认可取值：uuid、simple-uuid、random-32、random-64、random-128、tik）
  token-style: uuid
  # 是否输出操作日志
  is-log: true
# knife4j的增强配置，不需要增强可以不配
knife4j:
  enable: true
  setting:
    language: zh_cn
ai:
  dashscope:
    qian-wen-api-key: sk-0363d3e4787e4ab19253e56309e0ff95
    tongyi-xiaomi-analysis-pro: tongyi-xiaomi-analysis-pro
    qwen3_5_122b_a10b: qwen3.5-122b-a10b
    qwen3_5_omni_plus_2026_03_15: qwen3.5-omni-plus-2026-03-15
    qian_wen_base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
    qwen3_5_plus: qwen3.5-plus
    dashscope_qwen_image_2_0: qwen-image-2.0
    deepseek-v4-flash: deepseek-v4-flash
    qwen3_6_flash_2026_04_16: qwen3.6-flash-2026-04-16
  zhi-pu:
    zhi-pu-api-key: 4326f6d62899f7262aca39ef10a0aebd.WqS1NPb82t5ylEjD
    zhi-pu-base-url: https://open.bigmodel.cn/api/paas/v4/
    GLM-image: GLM-Image
  gateway:
    url: http://localhost:4000
    litellm_master_key: sk-global-litellm-key
typesense:
  agreement: http
  host: 8.140.219.233
  port: 8108
  apiKey: Wangjian020426


minio:
  endpoint: http://8.140.219.233:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: campus-future
  downloadUrl: http://localhost:8888/campus/help/object/download/
logging:
  level:
    com.wj.future.campus.mapper: debug
    # LangChain4j 日志 - 用于调试工具调用
    dev.langchain4j: DEBUG
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
    map-underscore-to-camel-case: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: ${spring.application.name}
langfuse:
  username: campus
  password: sk-xb-cat-sleep-123
mcp:
  amap:
    api-key-url: https://mcp.amap.com/sse?key=3c6e116912fe38c987139ef48caec428